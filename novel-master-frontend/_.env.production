# .env.production — Frontend Production Environment
# These are baked into the build at compile time

# API base URL: empty string = same-origin (nginx handles proxying)
VITE_API_URL=""

# WebSocket URL: empty string = same-origin
VITE_WS_URL=""

# App name (used in meta tags, notifications)
VITE_APP_NAME="Novel Master"
